---
import FloatingHeader from './site/FloatingHeader.astro';
import MobileNavDock from './site/MobileNavDock.astro';

interface Props {
  currentPage?: string;
}

const { currentPage = '' } = Astro.props;
---

<FloatingHeader currentPage={currentPage} />
<MobileNavDock currentPage={currentPage} />
