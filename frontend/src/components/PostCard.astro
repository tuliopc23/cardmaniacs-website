---
import ArticleCard from './site/ArticleCard.astro';

interface Props {
  href: string;
  title: string;
  summary?: string;
  date?: string;
}

const { href, title, summary, date } = Astro.props;
---

<ArticleCard href={href} title={title} summary={summary} date={date} />
