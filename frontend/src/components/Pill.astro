---
import UIPill from './ui/Pill.astro';

interface Props {
  variant?: 'default' | 'accent' | 'muted';
  class?: string;
}

const { variant = 'default', class: className = '' } = Astro.props;
---

<UIPill variant={variant} class={className}>
  <slot />
</UIPill>
